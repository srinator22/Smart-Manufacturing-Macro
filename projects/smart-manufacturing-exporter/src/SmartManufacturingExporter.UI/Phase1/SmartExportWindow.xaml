<Window x:Class="SmartManufacturingExporter.UI.Phase1.SmartExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Smart Export"
        Width="880"
        Height="560"
        MinWidth="700"
        MinHeight="440"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock FontSize="20" FontWeight="SemiBold" Text="Smart Export" />
            <TextBlock Margin="0,6,0,2" FontWeight="SemiBold" Text="Active assembly" />
            <TextBlock Text="{Binding RootAssemblyPath}" TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Width="92" Padding="8,4" Click="SelectAll_Click" Content="Select All" />
            <Button Width="92" Margin="8,0,0,0" Padding="8,4" Click="SelectNone_Click" Content="Select None" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  HeadersVisibility="Column"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Width="70" Header="Export" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Width="220" Header="File" Binding="{Binding DisplayName}" IsReadOnly="True" />
                <DataGridTextColumn Width="90" Header="Quantity" Binding="{Binding Quantity}" IsReadOnly="True" />
                <DataGridTextColumn Width="*" MinWidth="280" Header="Source" Binding="{Binding SourcePath}" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,14,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Destination" />
            <TextBox Grid.Column="1" MinWidth="300" VerticalContentAlignment="Center" Text="{Binding DestinationDirectory, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="2" Width="88" Margin="8,0,0,0" Padding="8,4" Click="Browse_Click" Content="Browse..." />
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Margin="0,0,16,0"
                       VerticalAlignment="Center"
                       Text="{Binding StatusMessage}"
                       TextWrapping="Wrap"
                       AutomationProperties.LiveSetting="Polite" />
            <Button Grid.Column="1"
                    Width="112"
                    Padding="10,6"
                    IsDefault="True"
                    IsEnabled="{Binding CanExport}"
                    Click="Export_Click"
                    Content="Export STEP" />
        </Grid>
    </Grid>
</Window>
