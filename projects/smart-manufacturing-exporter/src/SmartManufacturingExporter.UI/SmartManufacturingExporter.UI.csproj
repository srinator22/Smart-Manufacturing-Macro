<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\SmartManufacturingExporter.Application\SmartManufacturingExporter.Application.csproj" />
    <ProjectReference Include="..\SmartManufacturingExporter.Core\SmartManufacturingExporter.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Resource Include="..\..\assets\ribbon\smart-export-light-32.png" Link="Assets\smart-export-light-32.png" />
  </ItemGroup>

  <!-- Lets the test assembly see internal members (SmartExportTreeNodeViewModel.AggregationCount) so
       the aggregation-work-bound gate can assert on selection aggregation work without making the
       counter public API. -->
  <ItemGroup>
    <InternalsVisibleTo Include="SmartManufacturingExporter.UnitTests" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <UseWPF>true</UseWPF>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
