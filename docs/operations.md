# Operations

There is no hosted deployment target.

Each project defines its own release artifact. The first planned release unit is a signed, versioned, per-user Smart Manufacturing Exporter add-in package published through GitHub Releases after the same commit passes CI and Inventor integration testing. Installation, rollback, and monitoring procedures will be added with the first installable artifact. Until then, no build output is described as deployed or production-ready.
